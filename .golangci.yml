run:
  timeout: never

linters:
  disable-all: false 
  enable:
    - deadcode live socket
    - dupl
    - gofmt
    - goimports
    - gosimple
    - govet
    - ineffassign
    - misspell
    - nakedret
    - revive
    - structcheck
    - unused
    - varcheck
    - staticcheck

linters-settings:
  gofmt:
    simplify: true
  dupl:
    threshold: 10000
